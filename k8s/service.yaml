apiVersion: v1
kind: Service
metadata:
  name: self-healing-api-service
  labels:
    app: self-healing-api
    release: monitoring-stack
spec:
  selector:
    app: self-healing-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: NodePort